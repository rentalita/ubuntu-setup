server {
    listen 80;
    server_name localhost;

    root /srv/www/site;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    include /etc/nginx/conf.d/*.conf;
}
